#!/system/bin/sh
RMWTqPzqBZCP3LT893jwxwNhEbs6umRGWw

# Đảm bảo script có quyền root
if [ "$(id -u)" != "0" ]; then
  echo "This script must be run as root" 1>&2
  exit 1
fi

# Kiểm tra xem các tệp có tồn tại không
if [ -e /sys/devices/system/cpu/cpu0/cpufreq/scaling_max_freq ]; then
  # Đặt tốc độ tối đa của CPU
  echo "1.8 GHz" > /sys/devices/system/cpu/cpu0/cpufreq/scaling_max_freq
else
  echo "scaling_max_freq file not found!"
fi

if [ -e /sys/devices/system/cpu/cpu0/cpufreq/scaling_min_freq ]; then
  # Đặt tốc độ tối thiểu của CPU (nếu cần)
  echo "1.2 GHz" > /sys/devices/system/cpu/cpu0/cpufreq/scaling_min_freq
else
  echo "scaling_min_freq file not found!"
fi

echo "CPU frequency settings applied."
